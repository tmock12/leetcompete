= link_to "Delete Competition", user_competition_path(competition), method: :delete, confirm: "Are you sure?"
- competition_attributes.each do |key, value|
  %dl
    %dt= key
    %dd= value

= gmaps4rails(competition_map)
